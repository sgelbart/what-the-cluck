<%= form_for(@hatchery) do |f| %>
  <% if @hatchery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hatchery.errors.count, "error") %> prohibited this hatchery from being saved:</h2>

      <ul>
      <% @hatchery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
